apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: roboshop-dev
  region: us-east-1

managedNodeGroups:
  - name: roboshop-dev-managed
    instanceTypes: ["t3.small"]
    spot: false
    desiredCapacity: 2
